// Add this line to settings.gradle.kts to register the new dedicated
// architecture-test module. Adjust the path/name to match this project's
// module-naming convention (e.g. ":architecture-tests" instead of
// ":konture-test" if that fits better).

include(":konture-test")
